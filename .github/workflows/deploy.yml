name: Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - main # Запуск при пуше в ветку main

permissions:
  contents: write # Нужно для записи билда в ветку gh-pages

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Папка, которую нужно загрузить на сайт
          branch: gh-pages # Ветка, где будет храниться билд

